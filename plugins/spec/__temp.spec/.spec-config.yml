version: 3.3.0
paths:
  spec_root: ".spec"
  state: ".spec/state"
  memory: ".spec/memory"
workflow:
  auto_validate: true
  auto_checkpoint: true
  protect:
    - ".env"
    - "package-lock.json"
    - ".git/"
