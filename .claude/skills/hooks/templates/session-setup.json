{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "cd \"$CLAUDE_PROJECT_DIR\" && { [ -f package.json ] && npm ci --silent 2>&1 | tail -5; [ -f requirements.txt ] && pip install -r requirements.txt --quiet 2>&1 | tail -5; [ -f .env.example ] && [ ! -f .env ] && cp .env.example .env; } || true",
            "timeout": 300
          }
        ]
      }
    ]
  }
}
