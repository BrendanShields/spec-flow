{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "FILE=$(jq -r '.tool_input.file_path // empty'); if [ -n \"$FILE\" ]; then case \"$FILE\" in *.js|*.jsx|*.ts|*.tsx) npx prettier --write \"$FILE\" 2>/dev/null || true ;; *.py) black \"$FILE\" 2>/dev/null || true ;; *.go) gofmt -w \"$FILE\" 2>/dev/null || true ;; esac; fi"
          }
        ]
      }
    ]
  }
}
