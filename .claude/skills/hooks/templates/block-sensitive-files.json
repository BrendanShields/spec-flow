{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Read|Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "FILE=$(jq -r '.tool_input.file_path // empty'); if [ -n \"$FILE\" ]; then if echo \"$FILE\" | grep -qE '\\.(env|secret|key|pem|p12)$|credentials|password'; then echo '{\"decision\": \"deny\", \"reason\": \"Sensitive file access blocked for security\"}' >&2; exit 2; fi; fi; exit 0"
          }
        ]
      }
    ]
  }
}
